# Glonari Global Scene — Product Requirements Document

Status: DRAFT — planning only, no implementation performed.
Date: 2026-08-17

---

## 1. Product overview

Glonari Global Scene is a single-screen, illustrated "city scene" web experience. A composited skyline (sky + seven buildings + a foreground plaza) forms one visual scene, transferred from Figma into a static HTML/CSS page. The long-term product intent (per this planning request) is to evolve this static composition into an interactive experience: a branded loading sequence, a softly cursor-reactive scene, and select interactive building layers that open panels/modals — while preserving the original Figma composition as the visual foundation.

## 2. Product goals

- Preserve the Figma-derived visual composition exactly (composition, proportions, imagery, typography, hierarchy) while layering in interactivity.
- Introduce a distinct Loading Screen system ahead of the main scene ~~(design TBD)~~.
  > **SUPERSEDED (2026-08-25)** — design decided and implemented as a branded intro overlay. See §8 for the full resolved spec and DEVELOPMENT_PLAN.md Stage 4's IMPLEMENTED note for what shipped.
- Give the main scene a slow, soft, elegant, mouse-reactive global movement (reference: Aircord.co.jp hero, first screen only) — all layers move together as one unit, not classic multi-speed parallax. **SUPERSEDED (2026-08-18):** see §10 — replaced by per-layer depth parallax, requested explicitly by the user.
- Make individual buildings independently interactive (hover/click → future panels/modals) without those local effects fighting the global movement.
- Do all of this in controlled, independently shippable stages — not one large rewrite.

## 3. Current state of the project

The project is a minimal static site consisting of exactly three things on disk:

```
Glonari-Global-Scene/
├── index.html      (51 lines)
├── styles.css       (134 lines)
└── assets/
    ├── sky.png
    ├── global-connections.png
    ├── glonari-news.png
    ├── pulse.png
    ├── persona.png
    ├── banker.png
    ├── global-dream.png
    ├── glonari-ancillary.png
    └── plaza.png
```

There is no JavaScript file anywhere in the project. There is no build tool, bundler, package.json, framework, or dependency manifest of any kind. There is no README, no `.git` directory (not a version-controlled project yet), and no other configuration files. This is confirmed by direct directory inspection, not assumed.

The page currently renders a single static image composition with zero interactivity, zero animation, and zero JavaScript-driven behavior.

## 4. Existing technical structure

**Stack:** Plain HTML5 + plain CSS3. No preprocessor, no framework, no JS runtime dependency.

**Markup (`index.html`):** One root `<div class="scene" data-name="Glonari-Global-Scene">` containing 9 child `<div class="layer …">` elements in DOM order:
1. `.sky` — background sky image
2. `.building.building-global-connections`
3. `.building.building-glonari-news`
4. `.building.building-pulse`
5. `.building.building-persona`
6. `.building.building-banker`
7. `.building.building-global-dream`
8. `.building.building-glonari-ancillary`
9. `.plaza` — foreground plaza/fountain image (rendered last = visually on top)

Each layer wraps a single `<img>`. Seven of the nine images use a `.fill.cover` class (`position:absolute; inset:0; object-fit:cover`); two (`glonari-ancillary`, `plaza`) use bespoke crop wrapper classes (`.crop-ancillary`, `.crop-plaza`) that oversize and offset the image inside an `overflow:hidden` container instead of using `object-fit`.

**Layout/positioning (`styles.css`):** Every layer is positioned with `position:absolute` and percentage-based `left/top/width/height`, with CSS comments giving the exact originating Figma pixel values (e.g. `/* 2 / 2050 */`), confirming the coordinates were computed directly from a **2050×800 Figma frame**. The root `.scene` uses `aspect-ratio: 2050/800`, `width:100%`, `max-width:2050px`, `overflow:hidden` to scale the whole composition fluidly while locking its aspect ratio. `<body>` is a flex container centering `.scene` horizontally, with a navy `#0b1e3a` background filling any space beyond `max-width`.

**No JS, no animation, no media queries, no z-index** anywhere. Stacking order is entirely DOM-order-dependent. All images carry `pointer-events:none`, so nothing on the page currently responds to mouse or touch input.

## 5. Visual preservation requirements

The following must be preserved unless a future requirement explicitly overrides them:
- Overall composition and layer stacking order (sky → buildings left-to-right → plaza foreground).
- Every layer's relative position, size, and proportion as derived from the 2050×800 Figma frame.
- All existing image assets, at existing resolution/content (no placeholder swaps).
- The fluid, aspect-ratio-locked scaling behavior of `.scene`.
- The two bespoke crop techniques on `glonari-ancillary` and `plaza` — these are deliberate Figma-accurate crops, not bugs, and must not be "simplified" to plain `object-fit:cover` without checking that the visual result still matches Figma.

## 6. Main user experience

A visitor loads the page, sees a Loading Screen ~~(design TBD)~~ while assets/scene are prepared, then transitions into the main scene: a skyline illustration that subtly, smoothly shifts in response to mouse movement (soft global drift, not tracking), within which specific buildings can later be hovered/clicked to reveal more information via panels or modals. Exact panel/modal content and triggers are not yet defined.

> **SUPERSEDED (2026-08-25)** — the Loading Screen is no longer undefined: a full-screen branded intro (deep navy background, centered thin gold circular mark, "WELCOME TO" / "GLONARI") shows on every normal load/reload of either desktop or mobile, holds until the Campus is ready (capped at a 3.5s hard ceiling), then fades away to reveal the already-loading Campus underneath. See §8 for the full spec.

## 7. Functional requirements

Confirmed functional scope for the eventual product (not all in the current build):
- FR-1: Display the main scene (exists today, static).
- FR-2: ~~Display a Loading Screen before the main scene is interactive (does not exist today).~~
  > **SUPERSEDED (2026-08-25) — IMPLEMENTED.** FR-2 now exists on both desktop and mobile: a shared branded intro overlay (`intro.css`/`intro.js`) blocks/covers the main scene until it fades out. See §8 and DEVELOPMENT_PLAN.md Stage 4 for the full spec/decision record.
- FR-3: Global mouse-reactive movement of the entire scene as one unit (does not exist today).
- FR-4: Per-building hover state (scale/glow/highlight) (does not exist today).
- FR-5: Per-building click interaction opening a panel/modal (does not exist today; content undefined).
- FR-6: Reduced-motion support for FR-3/FR-4 (does not exist today).
- FR-7: ~~Touch/mobile equivalent behavior where cursor-based effects don't apply (does not exist today).~~
  > **SUPERSEDED (2026-08-21) — RESOLVED.** Decided: no cursor-parallax/hover equivalent exists or will exist on touch. Instead, tap-target hit zones over each building open the same desktop modal (`#modal-overlay`/`#modal-card`) with that building's content — tap-to-open, not tap-to-hover. See §19 and DEVELOPMENT_PLAN.md Stage 14. Not yet implemented as of this date.
  > **SUPERSEDED (2026-08-21, later same day) — IMPLEMENTED.** FR-7 now exists in `mobile/` (tap zones + shared `section-modal.js` modal). See §19 and DEVELOPMENT_PLAN.md Stage 14 for what shipped and the still-open real-device manual test.

## 8. Loading Screen requirements

Confirmed:
- It is a separate system/product from the main scene, sitting above/in front of it until dismissed.
- It must block interaction with the main scene while active.
- It must transition out once the main scene is ready, handing off control cleanly.

OPEN QUESTIONS (visual design and exact behavior not yet defined — do not invent):
- Visual design of the loading screen (layout, branding, imagery, colors, typography).
- Whether it shows a determinate progress indicator (real asset-load percentage) or an indeterminate/decorative animation.
- Minimum display duration (e.g., is there a designed minimum time even if assets load instantly)?
- Exact entrance and exit animation/transition style.
- What triggers "main scene ready" — all 9 images decoded? DOM ready? A fixed timer? Some combination?
- Error/fallback behavior if one or more assets fail to load (retry? skip? show error state?).
- Whether the loading screen reappears on revisit/reload or only on first visit (caching strategy).
- Accessibility requirements specific to the loading screen (e.g., `aria-live` progress announcements, focus handling).

> **SUPERSEDED (2026-08-25) — ALL OPEN QUESTIONS ABOVE RESOLVED.** Decided and implemented in the same pass as a shared branded intro overlay (`intro.css`/`intro.js`, linked from both `index.html` and `mobile/index.html` — see DEVELOPMENT_PLAN.md Stage 4's IMPLEMENTED note for the full file-level writeup). Resolution per bullet:
> - **Visual design:** full-screen overlay; background is the scene's existing deep navy (`#0b1e3a`, already used by `styles.css`'s `html,body`/`.stage-viewport`); a thin warm-gold (`--gold-bright`, the same accent as the custom cursor/building hover glow/section modal) circular mark centered in the viewport, with "WELCOME TO" above the more prominent "GLONARI" beneath/inside it. Minimal, architectural, no spinner/progress bar/buttons.
> - **Progress strategy:** decorative/indeterminate — no percentage or progress bar of any kind.
> - **Minimum display duration:** ~1.5s, even if the readiness signal below fires sooner.
> - **Exit transition:** a brief enlarge of the gold mark ("the Glonari world gently opening"), then a smooth opacity fade of the whole overlay to transparent, then full removal from the DOM (not left as an invisible blocker). Reduced-motion gets a plain fade with no enlarge/rotate.
> - **"Main scene ready" signal:** the page's own `load` event (or `document.readyState === 'complete'` if already true) — chosen because neither `main.js` exposes a custom readiness event, and this doesn't require inventing one; no per-asset `Promise.all`/`decode()` plumbing was added. Bounded by a hard ceiling of 3.5s so a slow/never-firing signal can't hold the intro open indefinitely.
> - **Failure/fallback behavior:** the 3.5s hard ceiling above doubles as the fallback — if `load` never fires (or fires late), the intro exits anyway and the Campus (already loading underneath the whole time) is simply revealed as-is; no retry or error state.
> - **Revisit/reload behavior:** shows on every normal load and every manual reload — explicitly not a once-per-session notification. The only suppression case is the destination page of an automatic desktop↔mobile device-gate redirect (Stage 14's `goMobile()`/`goDesktop()`, and only when called from their live `'change'`-listener, never the initial synchronous check): a one-shot `sessionStorage` marker is set immediately before that specific call and consumed (read + immediately cleared) on the destination page, so that one automatic navigation doesn't show the intro twice while every other path still does. See §19/DEVELOPMENT_PLAN.md Stage 14 for the device-gate this interacts with.
> - **Accessibility:** `aria-hidden="true"` on the overlay (transient/decorative, so screen readers proceed straight to the Campus); no `aria-live` announcements; no interactive controls; never steals or traps focus.

## 9. Main Scene requirements

Confirmed:
- The existing Figma-derived DOM/CSS structure is the starting point and should be preserved and extended, not rebuilt from scratch, unless inspection during implementation proves rebuilding is technically necessary.
- The 9-layer structure (sky, 7 buildings, plaza) already gives each building its own addressable container — this is a usable foundation for later hover/click work without markup duplication.

OPEN QUESTIONS:
- Should the scene support any content changes (new buildings, seasonal variants) beyond what exists today, or is the current 9-layer set final for this phase?

## 10. Global mouse-controlled scene movement requirements

Confirmed direction (from this request):
- Reference: **Aircord.co.jp**, first screen / hero only — motion feel, not literal visual design.
- NOT a multi-speed/multi-depth parallax system. All participating layers move together as a single unit; their relative positions to each other stay fixed during movement.
- Desired feel: smooth, soft, elegant, slow/eased, primarily horizontal with only slight vertical movement, not rigid 1:1 cursor tracking, with a soft curved/arc-like quality to the resulting motion path.
- Must return smoothly to a neutral/default position when the cursor leaves the active area.
- Must avoid exposing empty edges/background beyond the composition during movement (a real architectural constraint — see §22 and the Development Plan Stage 5).
- Must respect `prefers-reduced-motion`.
- Must degrade sensibly on touch/mobile where there is no persistent cursor.

OPEN / TO BE DEFINED (explicitly deferred to Development Plan Stage 6, after reference analysis):
- Exact cursor-to-offset mapping function and easing curve.
- Exact horizontal/vertical amplitude limits (in px or % of scene size).
- Smoothing/interpolation algorithm (e.g. lerp per frame vs spring physics).
- What produces the "curved/arc-like" quality mathematically (e.g. non-linear mapping, slight rotation, combined translate+rotate, or an actual curved path function) — NOT assumed yet.
- Whether movement is scoped to the whole viewport, or only an explicit "scene container" region.
- Any interaction with scroll (this is described as a first-screen/hero-style effect; whether the page scrolls at all beyond this scene is currently UNKNOWN).

> **SUPERSEDED / CORRECTED:** A first implementation of Stages 5–7 (fixed `--world-scale-x:1.12`/`--world-scale-y:1.04` headroom filled with a navy color buffer, `.stage-viewport` capped at `max-width:2050px`/`aspect-ratio:2050/800`, fixed `±4.5vw`/`±1.5vh` amplitude) was rejected: it rendered the desktop scene as a long low strip that did not fill the viewport height. The following are now **confirmed** requirements, replacing the corresponding "open" items above:
> - **Desktop scene fills the full browser viewport** — full width, full height, no empty strip above or below.
> - The 2050×800 Figma frame is the **logical/internal coordinate system** only, not the visible viewport size. Desktop rendering uses an `object-fit: cover`-equivalent fit (`scale = max(viewportWidth/2050, viewportHeight/800)`, plus a small overscan margin for safe vertical travel), computed per viewport and applied to a `.scene-world` layer sized in real pixels.
> - Because the Figma frame's aspect ratio (2.5625) is much wider than any realistic desktop viewport, the wide composition **intentionally** extends beyond the left/right viewport edges once it covers full height — this is correct, not a layout bug, and is the real space mouse movement explores.
> - Safe horizontal/vertical travel is derived from **actual rendered overflow** (`renderWidth/Height` vs viewport size), not from an artificial buffer layer or fixed percentage-of-viewport constants. Empty edges must never become visible, by construction (translate range is bounded by real overflow on all sides).
> - Cursor-to-offset mapping is linear per axis; horizontal amplitude is naturally dominant over vertical because of the composition's own aspect ratio, not because of different easing speeds per axis.
> - Smoothing is a single shared time-independent exponential damping constant (not per-axis differentiated tau).
> - This is not classic parallax: all 9 layers still move together via one shared transform on `.scene-world`.
> - A desktop custom cursor was added as part of this correction (not part of the original request): a small gold dot plus a softly trailing gold ring, `pointer-events:none`, shown only on fine-pointer/hover-capable devices (`(hover:hover) and (pointer:fine)`), fully independent of the scene's own movement transform. No custom cursor on touch/coarse-pointer devices.
> Implementation status: technically complete per this correction, verified mathematically and via automated headless-browser rendering — pending manual visual approval.

> **SUPERSEDED (2026-08-18)** — the "NOT a multi-speed/multi-depth parallax system... all layers move together as a single unit" requirement (both the bullet at the top of this section and the "not classic parallax" line in the correction block just above) is replaced by an explicitly user-requested **per-layer depth parallax**: sky, each of the 7 buildings, and plaza now each move at their own rate off the same shared eased pointer offset. This was a deliberate, explicit reversal requested in chat, not a bug or scope creep. Buildings' depths are derived mathematically from their real position in the 2050×800 frame (not hand-picked), so every building remains fully revealable at its own relevant pointer extreme; sky/plaza keep fixed reference depths (0.2 / 1.1). See DEVELOPMENT_PLAN.md Stage 7's matching supersession note for the full accepted architecture and verification method.

## 11. Aircord motion reference

The Aircord.co.jp hero was named as the feel reference, not a visual reference. A dedicated analysis stage (Development Plan Stage 6) must run before any movement code is written, producing a short spec of: what is being copied (feel, easing character, amplitude proportions) vs. explicitly not copied (their visual design, their specific imagery/layers, any scroll-linked behavior beyond the hero). This document does not itself contain that analysis yet — it has not been performed.

## 12. Individual interactive-element requirements

Confirmed:
- Buildings are the candidate interactive elements; each already exists as its own DOM node (`.building.building-<name>`), which is favorable for adding local interaction wrappers later.
- Global scene movement and local per-building hover/click effects are two separate systems and must be architected so neither's transform/animation logic overwrites the other's (see §22, and Development Plan Stage 8).
- Not every building will necessarily be interactive — the final interactive set is undecided.

OPEN QUESTIONS:
- Which of the 7 buildings (global-connections, glonari-news, pulse, persona, banker, global-dream, glonari-ancillary) are interactive, and which (if any) are decorative only?
  > **SUPERSEDED (2026-08-18)** — resolved: all 7 are interactive (hover confirmed and implemented); none are decorative-only.
- Is the plaza/fountain layer itself ever interactive, or purely foreground decoration?
- Is there a defined "hit area" per building that differs from its visual bounding box (e.g. a smaller/larger clickable zone)?
  > **SUPERSEDED (2026-08-18)** — resolved: the hit area is each building's real PNG alpha silhouette (sampled from an offscreen canvas per building against the live pointer position), not its rectangular bounding box. This is more precise than the `Glonari Global Campus` reference project's own approach, which uses hand-measured rectangular hotspots (it renders one flattened day/night image with no per-building alpha to sample) — feasible here specifically because each of the 7 buildings is already its own transparent-background image. See DEVELOPMENT_PLAN.md Stage 8's 2026-08-18 correction.

## 13. Hover behavior requirements

Confirmed desired effects (from this request), exact parameters undefined:
- Slight scale increase on hover.
- Glow/highlight/outline effect on hover.
- Smooth hover-in and hover-out transitions.
- A distinct visual "hovered" state.

OPEN QUESTIONS: exact scale factor, glow color/intensity, transition duration/easing, whether effects differ per building.

> **SUPERSEDED (2026-08-18)** — a first hover-only slice is now implemented and confirmed, at explicit user request scoped to glow only (scale and cursor change deliberately deferred as separate future steps, not decided against). Confirmed for this slice:
> - Glow: warm gold `filter: drop-shadow(...)` (two stacked shadows — tight ~10px + wide ~24px — `rgba(255,209,128,0.55)` / `rgba(255,178,90,0.35)`), which follows the building's real silhouette rather than a rectangular outline, consistent with §12's hit-area resolution above.
> - Transition: `0.25s ease` in and out, within the requested 0.2–0.3s range.
> - Uniform across all 7 buildings — no per-building variation.
> - Still open/unscheduled: scale factor on hover, and any cursor state change. See DEVELOPMENT_PLAN.md Stage 9's 2026-08-18 correction for implementation detail.
>
> **SUPERSEDED (2026-08-18, follow-up)** — scale factor now confirmed and implemented: `scale(1.03)` (within the requested ~1.02–1.04 range), uniform across all 7 buildings, `transform-origin: 50% 100%` so the building appears to lift off its own base. Applied to a new `.building-lift` wrapper element nested inside each building, never to the building's own parallax-carrying element (see DEVELOPMENT_PLAN.md Stage 9's matching follow-up note for why). The hovered building's z-index is also temporarily boosted above its siblings (but kept below the plaza's fixed foreground z-index) so the enlarged silhouette is never clipped by a neighboring building. Cursor state change remains the only still-open item from this section. ~~Known limitation: the Glonari Ancillary building's existing crop-technique `overflow:hidden` clips part of its glow/scale bleed on one edge — not fixed in this pass (see DEVELOPMENT_PLAN.md Stage 9 for detail).~~
>
> **SUPERSEDED (2026-08-18, second follow-up)** — the ancillary limitation above visibly clipped the building's roofline (not just a minor edge) and has been fixed: `overflow:hidden` moved from the building's parallax-owning element onto its `.building-lift` scale wrapper itself, so the crop window and the hover scale now transform together instead of the window staying fixed-size while its content grows. No remaining known limitation for any of the 7 buildings. See DEVELOPMENT_PLAN.md Stage 9 for implementation detail.
>
> **SUPERSEDED (2026-08-18, third follow-up)** — cursor state change now confirmed and implemented, at direct user request to reuse the Glonari Global Campus reference's own cursor-hover logic rather than a new design: `.cursor-dot` grows 8px→64px with a soft translucent gold fill on hover, `.cursor-ring` fades out at the same time — values carried over from that reference's `#cursor.hover`/`#cursor-ring` rules. No open items remain in this section.

## 14. Click behavior requirements

Confirmed: clicking a defined interactive building should trigger some UI response (very likely opening a panel or modal). Exact target list and exact resulting UI are undefined.

> **SUPERSEDED (2026-08-18)** — resolved, at direct user request: clicking a building opens a modal (see §15). Target list is all 7 buildings (§12). Hit-testing for the click reuses the same real-silhouette detection as hover (§13), not a separate bounding-box check.
>
> **SUPERSEDED (2026-09-21)** — one exception carved out of the rule above: the Digital Banker building (`.building-banker`) no longer opens the in-page modal. Clicking or Enter/Space-activating it now calls `navigateWithFade()` (`main.js`), which fades `#exit-transition` (a full-screen black overlay, sibling of `#modal-overlay`, 0.5s CSS transition) to opaque and then navigates to `digital-banker/gia/index.html` — the first screen of the separate Digital-Banker-GIA → Digital-Banker-Lobby → Global-Reserve sequence (three previously-independent sites, each still governed by its own project files, now reachable only by following this path from the Glonari scene). Implemented as a `NAV_TARGETS` lookup keyed by building class, checked before the modal-open fallback, so adding a future building→site link means one more map entry, not a new special case. The other 6 buildings are unaffected and still open the modal exactly as described above. Verified (this pass): every asset/script referenced along the connected path (this page → GIA → Lobby → Global Reserve) resolves over HTTP when served locally, and the inline navigation scripts pass a JS syntax check; an actual interactive browser walkthrough of the hand-off was not performed (no browser-automation tool was available in this session) — see DEVELOPMENT_PLAN.md Stage 10's matching note.
>
> **STATUS (2026-09-21, later same day)** — the walkthrough flagged above as not performed has now been run, live, in Playwright against the local dev server (`localhost:5173`): index.html → click Digital Banker → GIA → scroll to end → auto-advance → Lobby → click Global Reserve arch → arrival video loop → auto-advance → global-reserve/index.html, plus a control click on a non-Global-Reserve arch (Move Money) to confirm it still opens the ordinary modal instead of navigating. Both timed hand-offs were measured against their real-world elapsed time via a `MutationObserver` on the `#exit-transition`/`#gr-arrival` class changes (values persisted to `sessionStorage` so they survive the actual page navigation): GIA → Lobby fired at 1103.8ms against `ADVANCE_DELAY_MS = 1100` (digital-banker/gia/index.html:326), and the arrival-loop → Global Reserve hand-off fired at 1800.9ms against `RESERVE_HOLD_MS = 1800` (digital-banker/lobby/index.html:839) — both within one animation frame of spec. Zero console errors were produced by the GIA/Lobby/Global-Reserve pages themselves at any step. One real, unrelated bug was found and fixed in the same pass: the plaza fountain video referenced here as `assets/вода.mp4` (index.html:197) was 404ing on every load because the physical file on disk was literally named `#U0432#U043e#U0434#U0430.mp4` (an escaped-Unicode placeholder string, not real Cyrillic characters) instead of `вода.mp4` — renamed to match; the reference in index.html was already correct and needed no change. Two caveats on this pass: (1) the desktop building and lobby-arch hover/click were triggered via direct DOM event dispatch (`pointermove`/`pointerenter`/`click` at the real element coordinates) rather than literal OS-level mouse movement, because Playwright's default pointer-actionability check failed on both — on desktop the foreground `.plaza` layer sits in front of `.building-banker` at its bounding-box center (expected, matches the real-pixel-alpha hit-test design), and in the Lobby, `#arch-2`'s `getBoundingClientRect()` reports the element far outside the viewport (`x: -743px`) under its `matrix3d` corner-pin transform even though it renders on-screen — this second one has not been confirmed as a real user-facing hit-target/visual mismatch, just flagged as unverified by literal mouse-pixel targeting and worth a manual check; (2) the two mid-transition screenshots requested (during the Digital-Banker fade-out, and during the arch "flight") both landed after navigation had already completed by the time the screenshot call executed — the fades (550ms / ~3s total) were shorter than this session's tool round-trip, so only before/after frames were captured, not the transition frame itself.
>
> **SUPERSEDED (2026-09-21, later still)** — the "arrival video loop" described in the STATUS note above was a temporary placeholder concept and has been replaced: the looping `scenes/global-reserve/arrival-idle.mp4` (deleted from the repo) is now a static frame, `scenes/global-reserve/reserve-first-frame.jpg`, extracted (`ffmpeg -frames:v 1`) from the separate Global Reserve site's own hero video (`global-reserve/media/hero-vault.mp4`) — the vault-doors shot, 1280×720, no text baked into the frame itself (Global Reserve's on-screen text is separate HTML layered over its video, so nothing needed cropping/removing). `#gr-arrival-video` (a `<video>`) became `#gr-arrival-image` (an `<img>`) with the same `object-fit:cover` layout; the overlay's own GSAP cross-fade timeline and `active`/`gr-instant`/`video-visible` class logic (`digital-banker/lobby/index.html`) are unchanged — only what renders inside `#gr-arrival` changed. `RESERVE_HOLD_MS` stays at 1800ms (kept holding the static frame for the same duration the video loop held; not shortened). Re-verified via Playwright: clicking the Global Reserve arch mid-transition confirms `#gr-arrival-image`'s `src` resolves to `reserve-first-frame.jpg`, Escape mid-transition cleanly cancels/resets with no leftover references to the removed `<video>` element, the timed redirect to `global-reserve/index.html` still fires, and zero console errors were produced other than an unrelated `favicon.ico` 404. See chat/prompt from 2026-09-21.
>
> **SUPERSEDED (2026-09-21, even later)** — the `RESERVE_HOLD_MS = 1800` hold and the `#exit-transition` black fade that this hand-off used (both confirmed/instrumented in the STATUS note above) are gone for this specific hand-off. Rationale: `#exit-transition` exists to mask a visual seam between two *different* frames (it's still used exactly as before for the root-page → GIA hand-off in `main.js`, and within `digital-banker/gia/index.html` — neither touched by this change), but here the frame on both sides of the navigation is now the literal same image — `#gr-arrival-image`'s `reserve-first-frame.jpg` and Global Reserve's own `#scrub-video[poster]` (`global-reserve/media/hero-vault-poster.jpg`, a byte-identical copy, added this pass) are both `hero-vault.mp4`'s first frame. A black fade between two identical frames created a visible gap rather than hiding one. `RESERVE_HOLD_MS` dropped from 1800ms to 200ms — just enough to let `#gr-arrival`'s own GSAP crossfade (PHASE1+PHASE2+PHASE3, ~1.4s, unchanged) settle for a couple of paint frames before `window.location.href` fires directly, no fade-to-black step. The now-fully-unused `#exit-transition` element/CSS was removed from `digital-banker/lobby/index.html` only (it had no other caller in that file); the same-named element in the root page and in `digital-banker/gia/index.html` is untouched. Verified via Playwright with a `MutationObserver`+`sessionStorage` timing harness (same technique as the STATUS note above): crossfade reached `active video-visible` at 1415.3ms after click, redirect (`beforeunload`) fired at 1616.9ms — a 201.6ms hold, matching the configured 200ms within one frame. A side-by-side screenshot (lobby's settled arrival frame vs. Global Reserve's first paint) confirms the vault-door framing lines up pixel-for-pixel, with only Global Reserve's header/nav and hero text layered on top — no black flash, no jump cut. Escape pressed mid-hold (t=1500ms, i.e. after the crossfade settles but before the 200ms redirect fires) still cancels cleanly. Zero console errors on either page beyond an unrelated `favicon.ico` 404. See chat/prompt from 2026-09-21.
>
> **SUPERSEDED (2026-09-21, one more pass)** — correction to the note directly above: "lines up pixel-for-pixel" undersold a real, measurable residual jump that a rigorous pixel-diff (Playwright screenshots + Pillow/NumPy) caught even though it wasn't obvious to the eye at a glance. Two sources, both confirmed quantitatively, no others found (aspect ratio, scale/crop, and paint-timing were all checked and ruled out — a plain-background sample region showed a clean, spatially-uniform ratio, not the noisy pattern a scale/crop mismatch would produce): (1) Global Reserve's `.video-container::after` (`global-reserve/css/screens.css`) sits a permanent `rgba(0,0,0,0.24)` layer over Screen 1's video/poster; `#gr-arrival-image` in the lobby had no equivalent, so the exact same frame was ~24% brighter on the lobby side — confirmed by sampling a plain background patch, where the Global-Reserve/lobby pixel ratio measured 0.758–0.760 per channel, matching the CSS value's `1 − 0.24 = 0.76` almost exactly. Fixed by adding an identical `#gr-arrival::after { background: rgba(0,0,0,0.24) }` in `digital-banker/lobby/index.html`, painted over `#gr-arrival-image` via normal generated-content stacking (no markup change needed). Re-measured after the fix: the same patch's ratio moved to 0.996–0.999 (i.e. matched). (2) Global Reserve's fixed `.site-header` (`global-reserve/css/site-shell.css`, 72px, solid `--gr-void` background, no fade-in) has no lobby-side equivalent, so a header-height band appears instantly at the top of the frame on arrival — left as-is per instruction (Section 5's "Overlays Screen 1... no added padding or margin" is a deliberate standing decision, and `.site-header` is unmodified here and everywhere else); reported to the user as a separate, still-open, comparatively minor jump for them to decide on (e.g. a ~150–200ms fade-in scoped to arriving from this one hand-off) — not applied without further instruction. Whole-frame mean absolute per-channel pixel delta between the lobby's settled arrival frame and Global Reserve's first paint (1600×1000 viewport): **37.6 → 20.7** after the overlay fix (the remaining delta is almost entirely the still-open header band plus the hero text, which is expected/desired to differ — text is supposed to be different between "not yet arrived" and "arrived"). Restricting to the body below the header band: **32.4 → 16.1**; a diff heatmap of that region shows the fix leaves only the fading-in hero text and faint, sub-visible edge artifacts (median per-pixel delta 6–8 out of 255, consistent with `<img>` vs. `<video poster>` using slightly different `object-fit: cover` scaling/anti-aliasing paths for the same 1280×720 source, not a real color/brightness mismatch — this residual is not proposed as something to fix). See chat/prompt from 2026-09-21.
>
> **SUPERSEDED (2026-09-21, final pass)** — deliberate change of strategy, at direct user request: rather than continuing to chase the residual header-band jump and sub-pixel scaling noise documented in the note directly above by matching frames more closely (a technical/pixel-matching approach), the hand-off now hides the seam artistically with a full-screen golden light-flash timed to land exactly on the real cross-document navigation. Rationale given: two separate HTML documents painting independently have a structural floor to how closely they can ever match (the header-instant-appearance and sub-pixel scaling residuals are both consequences of that, not bugs to keep hunting), so a flash that fully covers the screen right when the browser actually swaps documents sidesteps the problem instead of narrowing it further. Added `#light-flash`, a `position:fixed; inset:0` layer with `background: radial-gradient(circle, #fff6e0 0%, #ffe9b8 40%, #cd7f32 100%)` — center matches `--gold-bright` (#ffe9b8), outer edge matches Global Reserve's own `--gr-bronze` (#cd7f32); the outer stop is deliberately opaque bronze rather than transparent (unlike a naive "light fades to nothing at the edges" gradient) so the layer fully covers the viewport, corners included, at peak opacity — a transparent edge would have left the exact corners of the header-jump/scaling-noise problem still visible through it, defeating the purpose. On the lobby side (`digital-banker/lobby/index.html`): after `#gr-arrival`'s crossfade settles and the existing `RESERVE_HOLD_MS` (unchanged, 200ms) elapses, `scheduleReserveRedirect()` now adds `#light-flash.active` (CSS transition `opacity 170ms ease-out`) and only calls `window.location.href` `FLASH_IN_MS` (170ms) later — so the actual page unload happens while the screen is already fully gold, not on a bare frame. On the Global Reserve side (`global-reserve/index.html`): the identical `#light-flash` is defined in an inline `<style>` block in `<head>` (not a linked stylesheet, not a JS-added class) with `opacity:1` as its default, so it's already fully opaque on the very first paint with no flash-of-unstyled-content gap; a small inline `<script>` right after it holds for `FLASH_HOLD_MS` (90ms, letting the header/video/text underneath finish rendering) then fades it out over `FLASH_OUT_MS` (300ms, ease-in) and removes the element, revealing the finished page as one smooth dissolve rather than a hard cut. `z-index:2000` there (vs. Global Reserve's highest prior z-index of 1000 on `.site-header`) and `z-index:100` in the lobby (above `#modal-overlay`'s 80) put it above absolutely everything on each page. Not treated as a new Approved Exception on the Global Reserve side: it is a separate full-viewport layer that never touches Screen 1's own HTML/CSS/JS, `.site-header`, or any locked file — the same category of non-modification as adding a sibling element, not an edit to a locked screen. Escape during any part of the lobby-side sequence — including mid-flash, before navigation fires — still cancels the whole trip cleanly (`cancelReserveRedirect()` now also clears the flash's own timer and removes `.active`), verified via Playwright by cancelling mid-ramp (t≈1716ms) and confirming no navigation occurred 1.5s later, flash opacity back at 0, zero console errors. Full sequence re-verified via Playwright with a `MutationObserver`+`sessionStorage` timing harness plus a series of precisely-targeted screenshots across the hand-off: crossfade settles at ~1404ms, flash starts at ~1605ms (RESERVE_HOLD_MS elapsed), `beforeunload` fires at ~1780ms (FLASH_IN_MS elapsed) with the screen already fully opaque gold at that instant (confirmed by a screenshot taken at that exact moment), the same full-opacity gold persists into the next document (confirmed on Global Reserve immediately post-navigation), and the page is fully revealed — header, vault frame, and hero text all in place, no gold tint remaining — by roughly 300–400ms after landing. At no captured instant in the sequence is either the raw brightness/scaling seam or the header's instant pop-in visible; the reveal reads as one continuous gold-to-clear dissolve. Total added time over the previous instant jump: ~0.5–0.6s, per the plan. Console clean on both pages beyond the unrelated `favicon.ico` 404. See chat/prompt from 2026-09-21.
>
> **SUPERSEDED (2026-09-21, GIA hand-off)** — the OTHER hand-off in this connected sequence, GIA → Lobby (`digital-banker/gia/index.html` → `digital-banker/lobby/index.html`, `ADVANCE_DELAY_MS = 1100` and `#exit-transition`, both instrumented in the STATUS note above), turned out to have a real loading-time problem, not just a bare-fade aesthetic one: the lobby scene's own first-paint assets (`layers/wall.png`, `layers/subjects.png`, `layers/Gia.png`, and the 6 `cropped-new/*.png` arch images — ~13MB combined) load in well under 200ms on localhost, but under a realistic throttled connection (Chrome DevTools Protocol network emulation, 10 Mbps / 40ms latency, measured against the machine's LAN IP rather than `localhost` — Chromium does not throttle loopback traffic, which made an initial `localhost` measurement falsely report near-instant loads) the full set took **~32.5 seconds** to finish downloading cold, versus the hand-off's own ~1.75s black-fade budget — confirming the reported black screen is a genuine asset-loading gap on ordinary connections, not merely a perception issue (though the plain fade-to-`--bg` was also inherently duller than it needed to be, which is the second half of the fix below). Two fixes, both applied: (1) **Prefetch during the scroll.** `digital-banker/gia/index.html` now fires `<link rel=preload>` for GSAP and all 9 lobby images once the visitor is halfway through the video (`p >= LOBBY_PREFETCH_AT = 0.5`), banking the real seconds still spent scrolling/reading as a download head start. Verified: after crossing the 50% mark and 3 real seconds of (throttled, 10 Mbps) dwell time, all 9 images had already finished downloading before the visitor even reached the bottom of the scroll. (2) **A light-flash hand-off, mirroring the lobby → Global Reserve one** (see the notes above), adapted to this pair's own dark palette instead of gold-to-bronze: `#light-flash` on the GIA side uses `radial-gradient(circle, #fff6e0 0%, var(--accent) 40%, var(--bg) 100%)` — `--accent` (`#D8B26A`, already documented in that file as "warm gold, sampled from the lobby light") as the mid-stop, `--bg` (`#07090F`) as the opaque outer edge — replacing the old plain fade-to-`--bg` `#exit-transition` (removed from this file; `main.js`'s and this file's own root→GIA hand-off pattern is untouched, since this `#exit-transition` had no other caller here). Timing mirrors the Global Reserve case exactly: `ADVANCE_DELAY_MS` (1100ms, unchanged) still holds on Block 4's final frame, then the flash ramps in over `FLASH_IN_MS` (170ms) before `window.location.href` fires. The lobby side gets a new, separate element, `#gia-entry-flash` (same gradient, opaque by default via an inline `<style>` in `<head>` so there's no flash-of-unstyled-content gap on first paint) — kept as its own element rather than reusing the lobby's existing `#light-flash` (which serves the *outgoing* trip to Global Reserve and needs the opposite default state, hidden-then-shown, plus a different gold-to-bronze gradient). Unlike the Global Reserve case, `#gia-entry-flash`'s hold is **not** a fixed short beat — since the images it's covering may genuinely still be mid-download despite the prefetch (e.g. a very fast scroller, or a slow connection where even a 2.5s-ish head start isn't enough), it instead waits for every one of `#wallLayer img, #subjectsLayer img, #giaLayer img, .arch-image` to fire its own `load`/`error` event (or to already be `.complete` when the script runs), with `MAX_HOLD_MS` (2200ms) as a hard ceiling so a stalled request can never leave it stuck; only once satisfied does it fade out over `FLASH_OUT_MS` (300ms ease-in) and remove itself. Verified via Playwright: with the preload's head start, images are already `.complete` by the time this script runs, so the flash begins fading within ~10–200ms of landing rather than waiting for the full ceiling; a precisely-targeted screenshot series across the whole sequence (Block-4 hold → flash ramps on GIA, opaque at the exact navigation instant → lands on lobby still opaque → fades over the following ~300ms) shows the *already-fully-loaded* lobby scene (Gia at her desk, the fountain, the arch labels) consistently visible through the thinning flash, never a blank/incomplete stage — full reveal by roughly 300–400ms after landing, matching the Global Reserve hand-off's own numbers. Zero console errors on either page in a clean verification pass (an unrelated `MutationObserver` bug in this session's own throwaway test-instrumentation script, not site code, briefly polluted an earlier console check — re-verified clean on a fresh page). See chat/prompt from 2026-09-21.
>
> **SUPERSEDED (2026-09-21, GIA hand-off, correction)** — the "3 real seconds... all 9 images had already finished downloading" claim in the note directly above was **wrong**, caught on request and re-measured properly: that number came from a browser context/profile already warmed by many earlier test runs in the same session, so images were served from disk cache almost instantly regardless of the simulated throttle — not a real measurement. Re-measured correctly this time: a brand-new `browser.newContext()` (no shared cache/cookies with any prior page), an explicit `Network.clearBrowserCache` over CDP before starting, and the same 10 Mbps/40ms throttle against the LAN IP. Honest result: after crossing the prefetch trigger, the full ~13MB set (GSAP + 9 images) takes **~13.5 seconds** to finish downloading — close to the ~10.85s a plain bandwidth calculation predicts (13.56MB ÷ 1.25MB/s), not 3 seconds. Two changes follow from this: (1) `LOBBY_PREFETCH_AT` moved from `0.5` to **`0.2`** (20% through the video) in `digital-banker/gia/index.html`, banking roughly 60% more of the remaining scroll/read distance as head start (80% of the scroll remains after the trigger instead of 50%) — this narrows the gap for a typically-paced visitor but, being bandwidth-bound rather than trigger-point-bound, does **not** shrink the ~13.5s figure itself, and still doesn't guarantee full coverage for a fast scroller or a slower-than-10-Mbps connection. (2) Because the prefetch alone isn't a guarantee, `#gia-entry-flash`'s hold logic (lobby side) was upgraded from a single hard `MAX_HOLD_MS` cutoff to a three-stage ladder: it waits for the real `load`/`error` event on every critical image (unchanged, and still the fast path whenever the prefetch had enough lead time); if that hasn't resolved within `LOADING_PULSE_MS` (1500ms after the script itself starts, which on a very slow connection can itself be delayed since the HTML document has to finish downloading first), it adds a `.loading` class that pulses the flash's opacity (`gia-entry-flash-pulse`, 1.6s ease-in-out, 1 ↔ 0.82) so an extended wait reads as "still loading," not frozen; `HARD_CEILING_MS` (15000ms) is the only forced cutoff, existing solely to stop a genuine network hang (a request that never fires load or error) from holding the flash forever. Verified via Playwright, clean context + cleared cache both times: at a deliberately pathological 1 Mbps throttle (chosen specifically to force the ceiling, since 13.56MB would take ~108s to finish there), the pulse activated as designed and the ceiling fired the forced reveal at ~15s after script start, fading and removing the element cleanly afterward — confirming the safety net actually engages rather than just existing in theory. At the realistic 10 Mbps throttle with the corrected `LOBBY_PREFETCH_AT = 0.2`, zero console errors, same clean hand-off behavior as before. See chat/prompt from 2026-09-21.
>
> **SUPERSEDED (2026-09-21, backward navigation added)** — this section's own established chain (Glonari campus → GIA → Lobby → Global Reserve) has been strictly one-directional up to this point: every hand-off documented above only ever moves forward, and none of those three connected screens previously had any way back except the browser's native Back button. A user request added explicit backward navigation to all three, each an **explicit hard-coded target URL, not `history.back()`** — Global Reserve has its own in-page anchor navigation (`#screen-2` etc. in `.site-header__nav`), and letting `history.back()` unwind through those anchor jumps instead of leaving the hand-off chain was explicitly the failure mode to avoid. Root `index.html` (Glonari campus) deliberately gets no back control — it is the first screen in the chain, nothing precedes it. Per screen: (1) `digital-banker/gia/index.html` — a new `.back-link` element (fixed top-left, `←` + "Back", same visual language as this screen's own `.cue` "Scroll" hint: uppercase Roboto, translucent dark pill with `backdrop-filter: blur`), `href="../../index.html"`, always visible on this screen (nothing here ever hides it). (2) `digital-banker/lobby/index.html` — the same visual treatment, `href="../gia/index.html"`, but hidden (`opacity:0; pointer-events:none`) via a `body.modal-active` class (newly added, toggled in the existing `openModal()`/`closeModal()` functions) while any building's placeholder modal (§15) is open, and via the pre-existing `body.gr-entering` class (added by `enterGlobalReserve()`, documented in the hand-off notes above) while the Global-Reserve-arch flight is in progress — reusing that existing class rather than adding a second one, since both moments already had a body-level flag. (3) `global-reserve/index.html` — no separate floating button (would duplicate/overlap the existing fixed `.site-header`, per that project's own CLAUDE.md scope rules); instead a minimal `←`-only `<a class="site-header__back">` was added inside `.site-header__inner`, immediately left of `.site-header__brand`, styled in `css/site-shell.css` under that project's Section 5.11 "Approved exception" (site-shell only, no Screen 1-10 content/copy touched), `href="../digital-banker/lobby/index.html"`. Verified via Playwright against a local static server: GIA's back-link navigates to the root campus page; the lobby's back-link navigates to GIA; clicking the lobby's Global-Reserve arch (`#arch-2`) still completes its existing flight/redirect unaffected by the new element; the Global Reserve header arrow navigates back to the lobby. The lobby's back-link was confirmed hidden (`opacity:0`, confirmed via `getComputedStyle`, not just the class present) immediately after opening a placeholder modal, and reappears once it's closed; separately, forcing `body.gr-entering` on an idle lobby load and waiting out its `0.3s` opacity transition confirmed the same element reaches `opacity:0`/`pointer-events:none` in that state too (this second check was done in isolation, since the real arch-2 flow completes its own redirect too quickly — under half a second, per the timings documented above — to reliably catch mid-flight in the same tool round-trip). Existing arch hover (`pointerenter` → `.is-hovered`) was re-checked and confirmed unaffected by the new markup. Zero console errors were introduced by any of the three new elements; a handful of pre-existing `MutationObserver`/lobby-image-preload-timing console messages observed during this pass are unrelated injected-tooling/pre-existing noise, not caused by this change (confirmed by grepping the edited files for `MutationObserver`, which finds none). See DEVELOPMENT_PLAN.md Stage 10's matching note. See chat/prompt from 2026-09-21.
>
> **SUPERSEDED (2026-09-21, persistent interaction hints added)** — three of the four connected screens (root `index.html`, `digital-banker/lobby/index.html`, and Global Reserve's Screen 1) previously gave no on-screen indication that they could be interacted with at all — the buildings/arches/scroll runway only revealed themselves to a visitor who happened to move the mouse or scroll. `digital-banker/gia/index.html`'s own `.cue` ("Scroll") was already the established visual language for this — Roboto, small uppercase caption, `~0.2em` letter-spacing, translucent white — so the same language was extended to the other three rather than inventing a new one: (1) Root `index.html` gets `.explore-hint` ("Explore"), fixed bottom-center, with a continuous opacity pulse (`0.55↔1`, the same `cueDrift`-style keyframe technique as GIA's own line-drift animation, just applied to the whole hint here since this screen's interaction never ends and there's no scroll/one-shot progress to fade it against). It has no hide-on-hover/click logic at all — the one deliberate exception is hiding while `#modal-overlay` is open, via a new `body.modal-active` class now toggled by `onOpen`/`onClose` hooks added to the shared `sectionModal.create()` call in `main.js` (the same class name `digital-banker/lobby/index.html` already uses for the same purpose, kept consistent rather than inventing a second name for the same concept). (2) `digital-banker/lobby/index.html` gets `.select-hint` ("Select a section"), same fixed position/pulse treatment, hidden via the two body classes that already existed on that page for `.back-link` (`modal-active`, `gr-entering`) — no new JS needed there, since both hooks were already in place. (3) Global Reserve's Screen 1 gets the scroll hint the approved build spec's own Section 5.1 had specified but never actually implemented (`GLOBAL-RESERVE-BUILD-SPEC.md`, `Scroll hint` row) — rather than the originally-specified longer caption, it now reuses GIA's `.cue`/`.cue__line` pattern verbatim (short "Scroll" caption plus a drifting vertical line), including that pattern's progress-based fade fix (see the entry directly below) instead of a fixed threshold; the copy substitution is recorded in `GLOBAL-RESERVE-BUILD-SPEC.md` itself per that project's own documentation-sync rule. No new fonts or dependencies were added anywhere — root `index.html` and the lobby reuse the same system font-stack (`-apple-system, Segoe UI, Roboto, Arial, sans-serif`) `digital-banker/lobby/index.html`'s own `.back-link` already established (no Google Fonts link exists on either of those two pages), while Global Reserve's hint uses `"Roboto"` directly since that page already loads it via Google Fonts for the Section 5.5 hologram-panel exception. Verified via Playwright: all three hints render visibly on first load; the root hint is confirmed hidden (`opacity:0`) while a building modal is open and reappears after close; existing arch/building hover and click behavior on both the root scene and the lobby were re-checked unaffected. See DEVELOPMENT_PLAN.md Stage 10's matching note. See chat/prompt from 2026-09-21.
>
> **SUPERSEDED (2026-09-21, GIA scroll-cue fix extended to Global Reserve)** — the same jarring, single-scroll-tick disappearance bug fixed on `digital-banker/gia/index.html`'s `.cue` (a hard `y > 24` pixel threshold driving a CSS class toggle) was deliberately not reintroduced when the equivalent hint was added to Global Reserve's Screen 1 in the entry directly above. `global-reserve/js/scrub.js` now carries the same `clamp01()`/`smoothstep()`/`ramp()` helpers GIA uses, and its existing `update()` function (already called on every scroll frame to set `latestProgress` from `getProgress()` — a local reading of `.chapters-wrapper`'s own scroll fraction, never the whole 10-screen page's scroll position) now also sets `#scroll-cue`'s opacity directly: `1 - ramp(progress, 0.045, 0.09)`, the identical tuned constants as GIA (fully visible through ~4.5% of this screen's own local progress, eased out by ~9%). Verified in-browser at nine progress points against `.chapters-wrapper`'s real scroll range: opacity reads `1` through `p=0.045`, smoothly interpolates (`0.741 → 0.417 → 0.259 → 0.126 → 0.034`) between `p=0.06` and `p=0.085`, and reaches `0` by `p=0.09`, holding there through `p=0.3` — a smoothstep curve, not a step function. See DEVELOPMENT_PLAN.md Stage 10's matching note. See chat/prompt from 2026-09-21.

## 15. Panels/modals future requirements

Explicitly out of scope for detailed specification now. To be defined once: (a) the interactive building list is finalized, and (b) content/copy for each panel is available. Architecture should not block this from being added later without rework (see §28, Out of scope).

> **SUPERSEDED (2026-08-18)** — partially unblocked at direct user request: the interactive building list is now finalized (all 7, §12), and a real modal *design* now ships — structure/style adapted 1:1 from the Glonari Global Campus reference's `#modal-overlay`/`#modal-card` (backdrop blur, card treatment, gold serif title, bordered close button), closing via close-button click, backdrop click, or Escape. Real *copy* is still not available: every building's description is explicit English-language placeholder text (2–3 sentences, concept-matched to its implied role), each marked `// TODO: replace with real copy` in `main.js`. The modal is a direct child of `<body>` (a sibling of `.stage-viewport`, never nested inside the parallax-transformed `.scene-world`), so it cannot move with the scene regardless of the pointer/parallax offset at the moment a building is clicked — verified directly. Still genuinely open: the real copy, and any panel content beyond a title + one paragraph.
>
> **Language policy confirmed (2026-08-18):** all user-facing text — modal titles, descriptions, any future labels/captions — must be in English regardless of the language a request or internal code comment is written in. This applies to the placeholder modal content above and to all future user-facing copy.

## 16. Animation and transition requirements

Confirmed motion principles to apply consistently across systems: smooth, soft, elegant, eased — not abrupt. Applies to: loading screen transition, global scene movement, hover in/out, and (later) panel/modal open/close. Exact easing curves/durations are undefined per system and will be set per-stage.

## 17. Performance requirements

Confirmed constraint: the movement and hover systems must be smooth (implying `requestAnimationFrame`-driven or CSS-compositor-friendly transforms, not layout-triggering properties). Not yet defined: target frame budget, device support floor, or whether image optimization (current assets are 1.5–2.5MB PNGs each, ~18.5MB total) is in scope for this phase — flagged as a risk in §27, addressed as its own stage (Development Plan Stage 15) rather than assumed now.

> **SUPERSEDED (2026-08-25)** — the image-optimization portion of this open question is answered: the format/compression half of Development Plan Stage 15 has been executed (see that stage for full detail). Every scene PNG, desktop and mobile, now ships a same-name `.webp` sibling (quality ~82, alpha preserved) wired through a `<picture>`/`<source type="image/webp">` element, with the original PNG kept as the `<img>` fallback (not deleted, not replaced with a placeholder). Total image payload across `assets/` + `mobile/assets/` fell from ~47.5MB to ~5.97MB (~87% smaller), with no visible quality change. Target frame budget, device support floor, and `requestAnimationFrame` profiling remain undefined/undone — this pass covered asset weight only, not the animation-loop half of Stage 15. See chat/prompt from 2026-08-25.

## 18. Responsive/layout considerations

> **SUPERSEDED / CORRECTED:** The behavior below described the pre-correction Stage 5 architecture (`aspect-ratio` + `max-width:2050px`, no breakpoints), which caused the scene to render as a short strip rather than filling the viewport. It has been replaced: the desktop scene now uses a JS-computed `object-fit: cover`-equivalent fit recalculated on every `resize`/`orientationchange`, so it fills the full viewport at any desktop window size, with the 2050×800 frame's overflow (mostly horizontal, given its wide aspect ratio) providing the mouse-movement travel space. This was checked mathematically and via rendering at 1440×960, 1920×1080, and 2560×1440; the full breakpoint matrix from Development Plan Stage 13 has not been run.

Confirmed prior behavior (historical; see correction above): the scene scaled fluidly via `aspect-ratio` + `max-width:2050px`, with no breakpoints — it was one fluid, proportion-locked layout, not a responsive reflow system.

> ~~Interim mobile exception added (2026-08-18): the cover-fit formula above is still exactly as described for viewports wider than 767px. Below that width it's temporarily overridden — see §19 — because the height-driven crop it relies on (safe only because a mouse can pan the cropped width back into view) hid most of the composition on narrow/portrait screens. Not a breakpoint-based responsive redesign, just a minimal, explicitly temporary exception pending the real Stage 14 decision.~~
>
> **SUPERSEDED (2026-08-18)** — that width-fit exception was rejected and reverted (it shrank the scene into a small letterboxed strip, not the desired look) and replaced with: the cover-fit formula above now applies unconditionally at every viewport size, mobile included — no breakpoint, no exception. See §19 for the current mobile behavior this produces.

## 19. Mobile/touch considerations

~~Confirmed: mouse-based global movement and hover have no direct touch equivalent. What replaces them on touch devices (static scene? tap-to-reveal instead of hover? tilt/gyroscope-based movement?) is UNKNOWN and explicitly deferred to Development Plan Stage 14. Do not assume gyroscope or tap-hover substitution without a decision.~~

> **SUPERSEDED (2026-08-21) — RESOLVED.** The question above is answered: mobile touch gets **no movement equivalent at all**, and never will — no gyroscope/tilt, no tap-to-simulate-hover. Confirmed replacement: a static mobile background with tap-target hit zones overlaid on each building's location; tapping a zone opens the same `#modal-overlay`/`#modal-card` modal used on desktop (see §15/§20, `main.js`'s `openModal(building)`) with that section's content, unchanged from desktop. No hover state exists on touch — tap goes straight to open. This is a decision record: as of this date `mobile/` has no tap zones or modal wiring yet (see the SUPERSEDED note below) — building it is Development Plan Stage 14's remaining, now-unblocked work.
>
> **SUPERSEDED (2026-08-21, later same day) — IMPLEMENTED.** The tap zones/modal wiring the note above still described as missing now exist. The modal's content/open/close/focus-trap code was pulled out of `main.js` into a shared `section-modal.js`/`section-modal.css` (desktop's `main.js` now calls into it too, instead of owning a second copy), and `mobile/index.html` gained 7 `.tap-zone` elements — one per building, positioned via `.tap-zone-*` rules in `mobile/styles.css` at percentages derived from real pixel measurements against `glonari-global-mob.png` — wired up by new `mobile/main.js`. Verified via an automated headless-browser pass driving every mobile tap zone and every desktop building: each opens the modal with the right title, moves focus to Close, restores focus to the trigger on close, and Escape also closes it, with zero console exceptions on either page. **Real-device manual testing has not been done** — see DEVELOPMENT_PLAN.md Stage 14 for the full implementation note and that outstanding gap.
>
> **SUPERSEDED (2026-08-23) — the flat `glonari-global-mob.png` + `.tap-zone-*` overlay described above has been replaced with a layered scene.** `mobile/` now composites the same shared `assets/sky.png`, `assets/plaza.png`, `assets/fountain.png`, and the 7 `assets/* frontal.png` building sources desktop uses (autocropped once per building into static `mobile/assets/buildings/*-frontal-cropped.png` files, then placed in a shared bottom-aligned frame with a per-building CSS scale multiplier — see DEVELOPMENT_PLAN.md Stage 14's matching note for the full spec and confirmed multiplier values). The building row is a plain horizontally-scrollable strip in source order, no swipe/carousel logic yet. Tap targets are now the visible `.building-frame` elements themselves (still carrying `data-building="building-*"`, still wired to the same shared `section-modal.js` open/close behavior via `mobile/main.js`) rather than invisible pixel-coordinate overlays on a flattened image. No hover state, no cursor parallax, no device-orientation movement was introduced — this rebuild is visual structure/asset placement only, resolving nothing about the still-open real-device manual verification gap noted above.
>
> **SUPERSEDED (2026-08-23, later same day) — the plain scrollable row above is replaced by a real looping carousel, built against a reference mockup.** One building is active/centered at a time (larger, full opacity/scale via a `.is-active` class); its immediate neighbors peek in at the screen edges (~45% of their width); prev/next arrows and 7 synced pagination dots were added; drag/swipe now pages the carousel one slide at a time (a JS-driven `transform`, with a live-follow drag and threshold-based snap) instead of native `overflow-x` list scrolling — see DEVELOPMENT_PLAN.md Stage 14's matching note for the full mechanism (clone-slide wraparound, band coordinates, verification detail). The ground-layer asset also changed from `assets/plaza.png` to `assets/background.png` (`plaza.png`'s own artwork already includes a fountain, which duplicated against the separate `assets/fountain.png` layer) — cropped once to `mobile/assets/background-cropped.png`. Tapping a side (peeking) building now re-centers the carousel on it; tapping the active/centered building is a deliberate no-op — modal wiring (`#modal-overlay`/`#modal-card`, `section-modal.js`) stays present in the markup but undriven, an explicitly deferred follow-up, not a regression. Side/¾-angle building imagery for non-active carousel positions is likewise an explicitly deferred follow-up: every slide still renders its `frontal.png`-derived source regardless of position.
>
> **SUPERSEDED (2026-08-23, later still)** — two proportion bugs in the carousel above, both scaling issues within the already-correct band split (not a change to the 0–37/37–63/63–100 split itself), fixed: (1) building images were effectively double-shrunk by two multiplied scale factors, most visible on Persona and Banker (the widest/lowest of the 7, which `object-fit: contain` was already letterboxing hard before any scale factor applied); (2) the ground layer was force-cropped via `object-fit: cover` into an arbitrary 37%-of-screen box, silently losing ~20% of its real pavement/steps content off the bottom and throwing the hedge/steps/pavement proportions off against the reference mockup. Fixed by rendering the ground layer at its true native 442px size (no cropping/distortion) and deriving the carousel band's height from that same constant instead of a fixed percentage — which also incidentally resolved most of the double-shrink symptom, since the resulting shorter carousel frame lets `object-fit: contain` alone bring most buildings to ~100% frame height. The fountain was also enlarged (`width: 32%→44%`, `bottom: 8%→16%`) to read as a proper focal element instead of a small shape crowded against the screen edge. Full before/after numbers and the retuned `--bldg-scale` values are in DEVELOPMENT_PLAN.md Stage 14's matching note.
>
> **SUPERSEDED (2026-08-24)** — the per-building `--bldg-scale` CSS multipliers described above are gone. The 7 `mobile/assets/buildings/*-frontal-cropped.png` files were replaced with new sources on a single shared 690×780 canvas, bottom-aligned, with each building's relative scale already baked into how much of that canvas it fills — the CSS no longer needs a compensating per-building factor. `mobile/styles.css`'s `--bldg-scale` custom property and its 7 per-building rules were removed entirely; `object-fit: contain` + `object-position: bottom center` (unchanged) still do the per-slot fitting. See DEVELOPMENT_PLAN.md Stage 14's matching note.
>
> **SUPERSEDED (2026-08-24, later same day) — the "side/¾-angle building imagery... explicitly deferred follow-up" note above (§19, the "plain scrollable row" entry) is resolved.** Non-active carousel positions now render each building's ¾-angled source (7 new `mobile/assets/buildings/*-angled.png` files, same 690×780 canvas/bottom-alignment as the frontal sources) instead of reusing the frontal image at every position. The active/side swap crossfades over ~340ms (two stacked `<img>` per slide, opacity-driven, `main.js` untouched — the existing `.is-active` class toggle drives it) rather than cutting instantly. Verified headless across all 7 buildings: correct face active in each position, 0 console errors, opacity sampled mid-transition confirms a genuine fade rather than a hard swap. See DEVELOPMENT_PLAN.md Stage 14's matching note, including a separately-flagged (not fixed) pre-existing bug: clicking the pagination dot for the already-active building permanently freezes further carousel navigation.
>
> **SUPERSEDED (2026-08-24, later still) — two point fixes against a reference mockup, both purely visual, no interaction/behavior change.** (1) Slots in `.carousel-track` now overlap by 70px (`.building-frame { margin-right: -70px }`, `main.js`'s `STEP = SLOT_WIDTH - OVERLAP`), closing most of the sky-colored empty space that previously showed between the active building and its peeking neighbors — slot *size* (`flex: 0 0 230px`) is unchanged, only the spacing between slots was reduced. (2) `.carousel`'s `top` moved from 254px to 274px (a 20px sink), so building bases now land under the ground layer's fixed top edge (514px, unmoved) instead of stopping exactly at the old seam — `.mobile-plaza`'s `z-index: 2` (already above `.carousel`'s `z-index: 1`) covers the overlapping ~20px, so buildings read as planted in the hedge line rather than floating just above it, matching the reference mockup. Verified headless across all 7 buildings, both as left and right carousel neighbor: no artwork clipping at the new overlap (each building's canvas has enough transparent padding beyond the guaranteed margin from the existing 60% side-building scale), 0 console errors, dot-click and drag/swipe paging both still function. Desktop untouched; no markup or interaction-wiring changes.
>
> **SUPERSEDED (2026-08-24, later still) — the 70px overlap above fully closed the gap instead of just reducing it, a regression, not the intended look.** 70px against the 230px slot width (~30% of slot width) left neighboring buildings touching/overlapping with zero visible sky between them — the "no clipping" verification in the note above checked that artwork wasn't cut off by `overflow`, which is a different property from whether a visible gap remained, so both that note and this correction are true simultaneously. Fixed: `OVERLAP`/`margin-right` reduced from 70px to **30px**. `top: 274px` (the hedge-embedding fix, point 2 above) is untouched. Verified via headless Chromium at 440×956, measuring each of the 7 buildings' actual non-transparent pixel bounds (not just DOM box edges) as both left and right neighbor of every other building: minimum visible gap ~26px, maximum ~54px — clearly visible on every pairing, noticeably smaller than the pre-70px (`OVERLAP` 0) spacing (~58–79px), and never zero or negative. The four cut/full-width buildings called out for extra scrutiny (Pulse, Global Dream, Glonari Ancillary, Glonari News) were each individually screenshotted as both left and right neighbor — no clipping, no edge overlap. 0 console errors. Desktop untouched (only `mobile/main.js`'s `OVERLAP` constant and `mobile/styles.css`'s `.building-frame { margin-right }` changed).
>
> **SUPERSEDED (2026-08-24, later still) — bug report: the ¾-angled face introduced two SUPERSEDED entries above was reused unmirrored on both peeking sides, so a building's front necessarily faced the wrong way on (at least) one side, instead of both peeking neighbors turning toward the centered building.** Fixed by distinguishing which side a peeking neighbor sits on (`main.js`'s `render()` now also toggles `is-prev`/`is-next` per frame, alongside the existing `is-active`) and mirroring `.building-face--angled` with `scaleX(-1)` (combined with the pre-existing `scale(0.6)`) on exactly one side per building. Which side needed the mirror was **measured, not assumed uniform**: an alpha-channel pixel scan of all 7 `*-angled.png` sources found they were not shot from one consistent camera angle — Banker/Global Dream/Glonari Ancillary are front-right renders (default: mirror `is-next`, correct as-is), Global Connections/Glonari News are front-left renders (the opposite convention, needing the opposite fix — `styles.css` scopes a higher-specificity override to just these two buildings), and Persona/Pulse are close enough to symmetric that either choice reads the same. **Known, disclosed tradeoff:** mirroring also flips any facade signage text the render carries, so the building's name reads backwards on whichever side ends up mirrored for it — unavoidable while reusing one 2D render for both sides, accepted because the wrong-facing defect this fixes is more visible at the peeking side's small/dimmed size than backward signage text is. Verified by reading the computed `transform` for every building's `is-prev`/`is-next` face across all 7 dot positions on the real files (each mirrored on exactly the side the measurement calls for, no exceptions) plus a full visual pass (no clipping/stretching) and 0 console errors. Desktop untouched; no markup, drag/swipe, or dot-click logic changed. See DEVELOPMENT_PLAN.md Stage 14's matching note for the full measurement numbers.
>
> **SUPERSEDED (2026-08-24, later still) — Pulse was wrongly called near-symmetric above and left out of the front-left override; it visually reads as the same front-left shot (receding side wall on the image's left edge) as Global Connections and Glonari News.** The pixel column-height scan behind the prior note's classification was unreliable specifically for Pulse — it was measuring decorative clutter (roof-terrace planters/parapet) at the sampled edge columns, not the building's real geometry — confirmed this time by direct visual comparison against `global-connections-angled.png` rather than by re-running that scan. `styles.css`'s front-left override block now also covers `.building-frame.building-pulse`, mirroring `is-prev` and leaving `is-next` unmirrored for Pulse, same as the other two front-left buildings; no change to the general default rule, `main.js`, desktop, or carousel spacing. Verified: computed `transform` re-checked across all 14 prev/next slots — Pulse now matches the front-left pattern, the other 6 buildings' transforms unchanged, 0 console errors, no clipping. See DEVELOPMENT_PLAN.md Stage 14's matching note.
>
> **SUPERSEDED (2026-08-24, later still) — every building was in fact turning away from the centered building on both peeking sides, confirmed by direct visual inspection of the live carousel, the opposite of the two prior notes' conclusions.** Fixed by a purely mechanical swap of `is-prev`/`is-next` across both `styles.css` rule blocks (the general rule and the three-building override) — the `scale(0.6)`/`scale(0.6) scaleX(-1)` values themselves are unchanged, only which side each applies to. Re-verified computed `transform` for all 14 prev/next slots against the immediately-prior state: every slot flipped mirrored↔unmirrored with zero exceptions, a complete and uniform reversal — which, combined with the confirmed premise that all 14 were wrong beforehand, means all 14 are now correct. This intentionally does not re-derive "which side" from pixel measurement or asset comparison again, since both of this section's prior attempts at that question turned out wrong; it applies the directly-observed correction instead. 0 console errors, no clipping. `main.js`, frontal/opacity crossfade, carousel gap/spacing, and desktop untouched. See DEVELOPMENT_PLAN.md Stage 14's matching note.
>
> **SUPERSEDED (2026-08-24, later still) — the carousel's pagination dots (`.carousel-dots`, `top: 520px`) read as lost right at the seam between the buildings and the hedge, rather than clearly against green.** Cause: `mobile/assets/background-cropped.png` has ~10–11px of fully transparent padding above its opaque content, so 520px (only 6px past the 514px carousel/ground seam) placed the dots in that transparent strip, not on hedge. Pixel-checked the actual asset (not assumed): the clean, fully-opaque, all-green hedge band spanning the dots' full centered width is image-relative y≈12–37; a staircase and two flanking planter curbs (with topiary taller than the curb) cut into that same width from y≈38 down — confirmed by first testing the reported 555–575px candidate range, which headless screenshots showed landing squarely on the cream staircase with the fountain globe intruding, not on hedge. Fixed: `top: 520px` → **`top: 540px`**, which — at the row's true rendered height of 7px (not 18px; only the active dot's *width* grows) — centers it at image-relative y26–33, solidly inside the clean hedge band with margin on both sides. `gap`, dot size/style, and `z-index` unchanged; desktop untouched. Verified headless: exact computed position matches (`top: 540, height: 7`), the row reads cleanly against solid hedge with no building/staircase/planter/fountain pixels touching it, 0 console errors across all 7 dot positions. See DEVELOPMENT_PLAN.md Stage 14's matching note.
>
> **SUPERSEDED (2026-08-24, later still) — real swipe/tap interaction implemented, closing the tap-to-open gap the "deliberate no-op" entry above left open.** Diagnosed and fixed three independent bugs (`mobile/main.js`/`mobile/styles.css` only) that made swipe/tap non-functional on a real device even though the carousel itself worked via the arrow buttons: (1) tap-vs-drag detection required movement to be *exactly* zero pixels, which a real finger (or even a real mouse) almost never produces, so a real tap was silently misclassified as a drag and swallowed; (2) `touch-action: pan-y` let the browser contest a not-perfectly-horizontal swipe as a native vertical-pan attempt and cancel it via `pointercancel`, even though `.screen` is a fixed-size box with nothing to vertically pan in the first place; (3) pressing on the building `<img>` elements triggered the browser's own native image-drag-and-drop once movement passed a few pixels, which stops delivering further pointer/mouse events to JS entirely until the drag ends — this specifically broke mouse-drag testing (item 6 below). With those three fixed, tapping the **active/centered** building now opens `window.SectionModal` (same shared module/content as desktop), resolving the "tapping the active building is intentionally a no-op" note earlier in this thread; tapping a peeking side building still just re-centers the carousel, unchanged. A visible press/touch-feedback state (`.is-pressed`, ~140ms scale-down + brightness lift while the pointer is down) was added to the tapped building, active or peeking. Prev/next arrows are unchanged and still present but are no longer the only functioning way to navigate — swipe/tap are now the primary mobile interaction, per this section's own confirmed requirement (§23) that mobile/touch be addressed, not left as an arrows-only fallback. Full root-cause detail and headless-Chromium verification are in DEVELOPMENT_PLAN.md Stage 14's matching note. Desktop untouched. **Real-device manual verification is still the one outstanding gap** — unchanged from every entry above since 2026-08-21.
>
> **SUPERSEDED (2026-08-24, later still) — bug report: tapping a peeking neighbor did nothing when that neighbor happened to be a wrap-clone slide rather than a real building** (the carousel's DOM has a clone of Pulse and a clone of Persona bracketing the 7 real slides for the seamless loop — see §19's 2026-08-23 entry — so the right neighbor while Pulse is active, and the left neighbor while Persona is active, are each a clone, not a real slide). Two deliberate early-returns added in the entry directly above — meant only to keep clones out of the *keyboard* path (`tabindex="-1"`/`aria-hidden`, unchanged and correct) — also blocked them as *pointer* targets, even though a clone is a real, visible pixel a finger/mouse can land on. Fixed: tapping/clicking a clone now pages the carousel to the same clone position `goTo()` already used mid-swipe, which resolves to the real slide it depicts exactly like the swipe path already did — it can never open the modal (a clone can't be the active slide at a moment a tap could reach it). `tabindex="-1"`/`aria-hidden` on clones are untouched; only pointer/mouse reachability changed. Verified via headless Chromium: for both directions, `document.elementFromPoint()` at the click coordinates first confirmed the resolved element genuinely carries `data-clone="true"` (reproducing the reported failure mode exactly, not just a nearby click), then a real click/tap at that point correctly paged to the real building each clone depicts, via mouse and via a genuine touch tap. Real (non-clone) side-building tap, active-building tap-to-open, and arrow navigation afterward are all unaffected. See DEVELOPMENT_PLAN.md Stage 14's matching note.
>
> **SUPERSEDED (2026-08-24, later still) — bug report: fast repeat swipes were silently dropped, most noticeably at the wrap-loop boundary.** A new swipe starting before the previous one's ~340ms transition finished used to be ignored entirely (`onPointerDown` bailed out while `isAnimating`) — three fast swipes in a row advanced the carousel by only 1 position instead of 3. Fixed by queuing a swipe's direction when it can't take live control of the track (a transition is already in flight) and replaying it once that transition's `transitionend` fires, chaining through as many queued swipes as actually arrived rather than capping at one and silently dropping the rest — the alternative of interrupting the in-flight transition and resuming live from its current position was rejected as needing extra, boundary-specific logic (the wrap-clone positions' resting transform doesn't numerically match the real slide they stand in for) for no real benefit over the simpler queue. **Separately fixed in the same pass:** one *continuous* drag (no release) pulled further than a slot past either buffer clone used to expose empty sky, since only 9 slides exist in the DOM; the live-drag render is now clamped to never show past either buffer clone, while a single gesture still commits exactly one step on release regardless of how far it was dragged, unchanged from before. Verified via headless Chromium with real pointer-event simulation (not mouse clicks) — three fast swipes with no pause advance by exactly 3 positions, checked in both wrap directions (Pulse→Global Connections forward, Persona→Glonari Ancillary backward), cross-checked with genuine CDP touch input (0 console errors); the clamp was verified by sampling the rendered transform throughout a 2000px continuous drag and confirming it never left the valid range. Tap-to-open/modal logic, the side-building mirroring, and carousel gap/`OVERLAP` sizing are untouched; desktop untouched. See DEVELOPMENT_PLAN.md Stage 14's matching note for full root-cause and verification detail.
>
> **SUPERSEDED (2026-08-25) — two further carousel tap bugs fixed, closing gaps in the tap-to-open/pointer-capture work introduced by the entries above.** (1) A tap landing while a previous swipe's transition was still in flight used to be silently dropped — the existing queueing (entry directly above) only ever replayed a *swipe's* direction, never a plain tap — so fast repeat taps on the active building could look like the carousel had hung. (2) The keyboard-only gold focus ring (`.building-frame:focus-visible`) could stay visible after an ordinary touch tap or mouse click, since `setPointerCapture()` suppresses the native `click` some mobile browsers rely on to tell pointer-driven focus apart from keyboard focus. Both fixed in `mobile/main.js` only — a same-slot tap queue replays once any pending swipes drain, and an explicit `blur()` scoped to the two pointer-driven tap call sites (not the shared activation function keyboard also uses, so Tab navigation's focus ring is unaffected). See DEVELOPMENT_PLAN.md Stage 14's matching note for full root-cause and verification detail.
>
> **SUPERSEDED (2026-08-25, later still) — bug report, measured precisely: a single continuous drag/swipe gesture hit a hard, silent dead stop with no feedback, once past a one-full-loop distance (measured at exactly 1400px for this carousel's current `STEP`).** This directly revises what the entry two above accepted as correct behavior — "a single gesture still resolves to exactly one step either way, unchanged from before" and a hard render clamp with no cue at the limit. Fixed with two changes in `mobile/main.js` (no `mobile/styles.css` change needed): (1) a continuous drag now commits however many whole `STEP`s it actually travelled (capped at one full loop from wherever it started), chained through the same `pendingSwipes` queue the fast-repeat-swipe fix already uses — short swipes under one `STEP` are unaffected and still commit exactly 1 step, matching prior behavior exactly; (2) the hard render clamp at the drag limit is replaced with rubber-band damping (a linear multiplier on the overflow past the limit), so the track keeps visibly responding instead of freezing, and releasing from inside that damped zone still animates smoothly back to the nearest valid slide via the carousel's existing transition, not an instant snap. Verified via real mouse-drag simulation (Playwright): a ~120px drag still commits exactly 1 step; a continuous 600px (3×`STEP`) drag commits exactly 3; a continuous drag to exactly one full loop's distance measured `transform` exactly at the theoretical limit, and continuing further produced a visibly different (damped, not frozen) value; releasing from well past the limit smoothly returned to the starting building via the existing chained transitions. Both previously-fixed tap bugs (dropped tap mid-transition, stray focus ring after tap) re-verified unaffected. See DEVELOPMENT_PLAN.md Stage 14's matching note for full root-cause, formula, and verification detail.
>
> **SUPERSEDED (2026-08-25, later still) — bug report with a screenshot: the active building visibly "jumps" right as it becomes active.** Not an asset-alignment issue this time (ruled out first — the reported building's frontal/angled bitmaps measured pixel-identical) but a `transform`-interpolation bug affecting all 7 buildings equally, on whichever peeking side is each one's mirrored side (§19's own 2026-08-24 mirroring entries above). Going active removes the mirrored side's `scaleX(-1)`, and the browser's default transition interpolates the underlying scale value straight through 0 rather than "flipping" — confirmed by sampling the live computed `transform` matrix during a real activation, which showed the scale component crossing zero mid-transition. Fixed in `mobile/styles.css` only, via two direction-specific `transition`-timing overrides on `.building-face--angled` (no change to any existing `transform` value or to `main.js`): the transform now holds its old value and snaps only once `opacity` has already faded below about half a percent (arriving at active), or snaps instantly from the very first frame while still fully transparent (leaving active for a mirrored peeking slot) — in both cases the snap lands while the face is functionally invisible, so nothing visibly changes about *when* it becomes the new value, only that it no longer visibly passes through a squashed, partially-mirrored state on the way. Verified across all 7 buildings on their own mirrored side: the smallest scale magnitude observed while still meaningfully visible never dropped below its resting 0.6 (previously it dropped to 0 every time). A known-safe, never-mirrored combination was also checked and confirmed unaffected. See DEVELOPMENT_PLAN.md Stage 14's matching note for the full root-cause and verification detail.

> ~~Interim fix, not a Stage 14 resolution (2026-08-18): the desktop cover-fit crop described in §16/§18 turned out to hide 6 of 7 buildings entirely on narrow/portrait viewports, since there is no mouse there to pan the cropped-off width back into view — reported as "only one building/picture visible on mobile." Below `max-width: 767px`, `main.js` now fits the full 2050px logical width to the viewport width instead of cover-fitting height, so the whole composition stays visible (smaller, letterboxed top/bottom) rather than cropped. This is explicitly temporary and minimal, requested to avoid a full responsive/touch redesign right now: it does not add tap-to-open, tilt-based movement, or any other touch interaction model — buildings are visible on mobile but still not reachable by touch for the modal (§15), since that click path still depends on mouse-only hover state (see §13/main.js). See DEVELOPMENT_PLAN.md Stage 14 for the still-open real decision.~~
>
> **SUPERSEDED (2026-08-18)** — the width-fit interim fix above was rejected and fully reverted (it shrank the scene into a small letterboxed strip, not the desired look) and replaced with a `cover`-style interim fallback instead: mobile now uses the exact same unconditional cover-fit formula as desktop (`scale = max(vw/2050, vh/800) * BLEED`), with no mobile-specific branch left in `main.js` or `styles.css` at all. On a portrait phone this fills the viewport by height and renders a large, centered fragment of the wide composition — the left/right portions of the scene fall outside the viewport and are clipped by `.stage-viewport`'s existing `overflow: hidden`, exactly like desktop's own intentional horizontal overflow, just with no mouse available to pan across it. The initial (and only, on touch) visible slice is the horizontal center of the composition, since the pan offset starts at `{0,0}` and nothing moves it without a mouse. Buildings are still not reachable by touch for the modal (§15) — that click path still depends on mouse-only hover state (see §13/main.js) — and a full mobile/touch adaptation is still not implemented. See DEVELOPMENT_PLAN.md Stage 14 for the still-open real decision.
>
> **SUPERSEDED (2026-08-21)** — the "unconditional cover-fit formula, no mobile-specific branch, no mobile-specific files" description above no longer matches what exists on disk. A standalone `mobile/` directory now contains its own `index.html`, its own `styles.css`, and a single flat, pre-composited image (`mobile/assets/images/glonari-global-mob.png`, 440×956), shown via plain CSS `object-fit: cover` inside a fixed-size `.screen` container — not the JS `scale = max(vw/2050, vh/800) * BLEED` formula this section previously described as applying unconditionally. There is no `main.js` in `mobile/`: no parallax, no pointer/touch movement, no hover, no click, no modal — it is a static picture, matching this section's own long-standing "UNKNOWN, explicitly deferred" framing rather than resolving it. Nothing in the root `index.html` or `main.js` links to, redirects to, or otherwise wires visitors into `mobile/` — the two trees are currently disconnected. This correction only brings the documentation in line with current reality; it is not an endorsement of `mobile/` as the final Stage 14 answer. See DEVELOPMENT_PLAN.md Stage 14 for the still-open real decision.
>
> **SUPERSEDED (2026-08-21, later same day) — the two trees are no longer disconnected.** Root `index.html` now carries a device gate that routes a visitor to one scene or the other; the two entry points aren't merged into one responsive page, but they're no longer silently independent either.
>
> **Approach chosen: a synchronous JS viewport-width check, not a CSS media query.** A `<script>` inline in `index.html`'s `<head>` — placed right after `<meta name="viewport">` (so the check already sees the real, post-viewport-meta device width, not a browser's pre-viewport-meta desktop-width default) and before every `<link rel="stylesheet">`/asset on the page — evaluates `window.matchMedia('(max-width: 767px)').matches` and, if true, calls `window.location.replace('mobile/index.html')`.
>
> **Why JS instead of pure CSS**, given the task named both as options: CSS media queries can show/hide/restyle content *within a single document*, but `index.html` and `mobile/index.html` are two separate documents/entry points with almost entirely different markup (parallax `.scene`/`.layer` tree vs. a flat image + `.tap-zone` overlay) — no CSS rule can navigate the browser to a different URL. Only script-driven navigation can actually route between them, which is what "leads the user to the desktop or mobile version" requires. The condition itself still reuses a CSS media-query expression (`matchMedia`), just evaluated in JS so its result can drive a `location` change rather than a style change.
>
> **Why 767px:** matches this project's own already-established mobile breakpoint (see the reverted `max-width: 767px` interim-fix attempts earlier in this section's history) — reusing the existing convention rather than inventing a second breakpoint number for the same concept.
>
> **Why in `<head>`, before stylesheets/assets, and not e.g. after full page load:** the point of the gate is that a phone-sized viewport should never load the desktop scene's assets at all (parallax layer images, `main.js`, `section-modal.js`) just to immediately discard them — running the check as the very first script, before the browser has requested anything else on the page, keeps that request cost off phones entirely (modulo the one already-necessary request for `index.html` itself).
>
> **Why `location.replace()` and not `location.href`:** `.replace()` swaps the current history entry instead of pushing a new one, so a mobile visitor's back button doesn't land back on `index.html` only to be redirected again — it goes to whatever they visited before this site.
>
> **Deliberately viewport-width-based, not touch/pointer-based:** the gate checks screen size, not input capability. A touch-enabled laptop with a wide screen is intentionally kept on the desktop scene (which already has its own independent fine-pointer/hover detection for the custom cursor, `finePointerQuery` in `main.js` — unrelated and unaffected by this gate) rather than being routed to the fixed-440px-wide mobile scene it would render badly on. This matches Stage 14's own decision scope: that decision was about narrow/portrait viewports having no mouse to pan with, not about touch capability in general.
>
> **Known, deliberate limitations (not silently incomplete):**
> - **One-directional only.** The gate lives in root `index.html` and sends narrow viewports to `mobile/index.html`; `mobile/index.html` has no matching reverse check, so a wide-viewport visitor who deep-links or resizes into `mobile/index.html` directly stays there. Not addressed here — flagged as an open item, not assumed away.
> - **Checked once on load, not on resize.** Resizing an already-loaded desktop window narrower does not trigger a live redirect — re-routing continuously while the user is actively resizing their own window would fight them, and desktop's existing `layout()`/`resize` handling in `main.js` already re-fits the scene to whatever width it's given. The gate decides which *scene* loads, once, not which layout renders at every width.
> - **No-JS fallback is the desktop scene.** With JavaScript disabled, the gate never runs and a phone gets the desktop parallax scene (functional per Stage 7–10, just not the intended mobile experience) rather than a broken page — a silent full-capability fallback, not a hard failure, but still worth naming as a real (if edge-case) gap.
>
> **SUPERSEDED (2026-08-25, later still) — bug report: the first limitation above ("checked once on load") is a real, reproducible gap, not just a theoretical one.** A page loaded wide (desktop scene) and then narrowed *without a reload* — a manual browser-window resize, an orientation change, or a backgrounded tab resuming at a different size — never redirected, since the one-time `matchMedia(...).matches` check only ever runs once, synchronously, at load. Fixed: the SAME `MediaQueryList` object used for that one-time check is now also kept and given a `'change'` listener (not a manual `resize` handler — `'change'` fires only when `matches` itself flips, not on every resize tick, so it costs nothing on the frequent case of resizing without crossing 767px). A crossing INTO mobile width is debounced 300ms — the pending redirect is cancelled if the state flips back before the delay elapses — guarding against a transient/flickering match (DevTools device-emulation toggling, or a brief moment of unstable `innerWidth` some mobile browsers report mid-orientation-change) without giving up reacting to a real, sustained resize. This directly revises the second bullet above ("the gate decides which scene loads, once, not which layout renders at every width") — it now also decides that, live, for the *one* direction (narrowing) that risks stranding a visitor on the wrong scene; desktop's own `layout()`/resize re-fit (mentioned in that same bullet) is completely unrelated to this and untouched.
>
> **Also addressed in the same pass: the first bullet above, "one-directional only."** `mobile/index.html` now carries a reverse gate — but deliberately **not** a mirror of the forward one. The forward gate (bullet immediately above the "Deliberately viewport-width-based, not touch/pointer-based" note two entries up) is intentionally screen-size-only; the reverse gate is deliberately **not** screen-size-only, because the risk profile is different in this direction. A phone rotated to landscape routinely exceeds 767px, and sending it to the desktop scene on that basis alone would be actively harmful — desktop's mouse-hover/parallax model has no touch equivalent, unlike the reverse (Stage 14 decided mobile itself intentionally gets no movement/hover equivalent — a different question from whether desktop's own model happens to work on a touchscreen; it doesn't). So the reverse gate requires **both** `(min-width: 768px)` **and** `(hover: hover) and (pointer: fine)` — the same primary-input media features `main.js`'s own `finePointerQuery` already uses for the desktop cursor decision, reused here rather than inventing a second detection philosophy for the same underlying question ("is this a mouse-driven device"). A touch-primary device never matches this, landscape or not, regardless of width, so the phone-rotation risk this section itself named is directly avoided; what it does rescue is the one case that's genuinely a desktop-experience gap: a mouse/trackpad user who loads (or is sent to) `mobile/index.html` with a narrow browser window and then widens it without reloading. Live via the same `'change'` + 300ms-debounce mechanism as the forward gate, and also checked once synchronously on load (closing the "deep-links directly into `mobile/index.html` stays there" half of the original gap too). **Known, accepted imprecision, same tradeoff `finePointerQuery` already carries on desktop:** `pointer`/`hover` reflect the *primary* input, so a touch+mouse hybrid device (e.g. a 2-in-1 laptop used via its touchscreen) can still match if its primary input is classified as mouse/trackpad — not solved here, and not a new problem this introduces. The third bullet above ("No-JS fallback") is completely unaffected by either change in this entry. See DEVELOPMENT_PLAN.md Stage 14's matching note for implementation and verification detail.

## 20. Accessibility requirements

Confirmed baseline already present: descriptive `alt` text exists on the building images. Not yet defined: keyboard-navigability/focus handling for future clickable buildings, ARIA roles for panels/modals, and screen-reader behavior around the loading screen and the moving scene (a scene that moves with the mouse should not disorient or trap keyboard/screen-reader users — needs explicit design).

> **Partial update (2026-08-18):** the modal shipped for §15 does carry ARIA roles (`role="dialog"`, `aria-modal="true"`, `aria-labelledby`/`aria-describedby`), and Escape closes it. What's still genuinely missing: buildings themselves are plain, non-focusable `<div>` layers — there is no keyboard path to open the modal at all (no `tabindex`, no Enter/Space activation), so a keyboard-only or screen-reader user cannot reach this feature yet. This is a real, disclosed gap against this section's own requirement, not an oversight being hidden.
>
> **SUPERSEDED (2026-08-18, follow-up)** — the gap above is closed, at direct user request. Checked the Glonari Global Campus reference first for existing focus-trap/keyboard logic to reuse, per instruction — it has none beyond focusing the close button on open and restoring the triggering element's focus on close (no actual Tab-trap), so that restore pattern was carried over and the trap itself was added new. Buildings now carry `tabindex="0"`, `role="button"`, and an `aria-label` (index.html); Enter/Space activates a focused building exactly like a mouse click (`main.js`); focusing a building shows the same glow/scale/z-index-boost as mouse hover, plus a `:focus-visible` outline exclusive to real keyboard focus. The modal now traps Tab/Shift+Tab inside `#modal-card` while open (with a `focusin` safety net for focus landing outside it by any other path) and restores focus to the exact building element that opened it on close — matching this section's own "should not disorient or trap keyboard... users" concern by ensuring focus always has a well-defined, reversible path in and back out. Not addressed by this update: screen-reader behavior around the moving scene itself (still open), and the loading screen (Stage 4, not yet built).
>
> **SUPERSEDED (2026-08-18, later same day)** — the `:focus-visible` outline mentioned above is removed (reported as a visual defect: a rectangular ring around a building's non-rectangular silhouette, visible right after modal-close focus restore). The focus indicator is now the glow+scale alone, already shared with mouse hover — no separate outline. Keyboard focusability/Tab order/Enter-Space activation/focus-trap/focus-restore are all unaffected; see DEVELOPMENT_PLAN.md Stage 8's matching note for verification detail.

## 21. Reduced-motion requirements

Confirmed: any global movement, hover animation, loading transition, and panel animation must respect `prefers-reduced-motion: reduce`, offering either no motion or a minimally-reduced variant. Exact reduced-motion behavior per system (e.g., does the scene stay static, or keep hover feedback without the scale/motion?) is to be defined per-stage, not assumed globally now.

## 22. Technical constraints

- No JavaScript exists yet — any interactivity requires introducing a script from scratch. Per this request's constraints, do **not** introduce a framework "just because it's convenient" — vanilla JS is the default assumption unless the user decides otherwise (flagged as a Technical Assumption, §25).
- `pointer-events:none` is currently set on all image elements — future hover/click work must deliberately re-enable pointer events only on the elements meant to be interactive, leaving decorative layers (sky, plaza) inert.
  > **SUPERSEDED (2026-08-18):** the hover slice that shipped does not re-enable `pointer-events` on building images at all — `main.js` does its own pointer-coordinate + PNG-alpha hit-testing (see §12), which never goes through the DOM's native pointer-event hit-testing chain. `pointer-events:none` remains exactly as it was on every image, unchanged. This constraint stays relevant for a future *click* implementation if that instead relies on native events rather than the same custom hit-testing approach.
- No `z-index` is declared anywhere — stacking is pure DOM order. A future Loading Screen overlay and future panels/modals will be the first elements requiring explicit stacking-context management.
  > **SUPERSEDED (2026-08-18):** no longer accurate. Sky/plaza now carry fixed `z-index` (0 / 1000) and each building carries a `z-index` proportional to its computed parallax depth — needed once per-layer parallax (see below) let adjacent buildings drift at different rates and otherwise cover each other's facade at their shared seam. Any future Loading Screen/panel `z-index` must be chosen above this existing range.
  > **SUPERSEDED (2026-08-18, follow-up):** a third z-index rule was added for the hover-scale slice (§13): the currently-hovered building's z-index is temporarily forced to a fixed `500` — above every building's normal depth-derived range (~55–110), below the plaza's fixed `1000` — so its enlarged hover silhouette isn't clipped by a neighboring building, while the plaza's own foreground compositing over building bases stays exactly as before. Reverts to the normal depth-derived value on hover-out. Any future Loading Screen/panel `z-index` must clear `1000`, not just this building range.
- The `.scene` element is exactly the size of the visual composition with `overflow:hidden`. Naively applying `transform: translate()` to `.scene` for global movement will expose the navy body background at its edges. This means global movement needs an architecture change — most likely an outer masking "stage" wrapper distinct from the moving `.scene`/composition layer — planned in Development Plan Stage 5, not implemented now.
  > **SUPERSEDED / CORRECTED:** resolved by the accepted full-viewport-mask + cover-sized `.scene-world` + real-overflow-derived travel architecture (see §10, §18). The moving layer is `.scene-world`, sized by JS to always exceed the viewport on the axis(es) needed for safe travel, so an edge can never be exposed by construction — no artificial buffer layer needed.
  > **SUPERSEDED (2026-08-18):** `.scene-world` is no longer the moving layer — it now stays statically centered/sized. Each `.layer` (sky, buildings, plaza) receives its own depth-scaled `translate3d()` off the same shared eased pointer offset (see §10's matching note and DEVELOPMENT_PLAN.md Stage 7). The "edge can never be exposed by construction" guarantee still holds, now re-derived per-layer: `maxTravelX/Y` is normalized against whichever layer has the highest depth, so no layer can exceed the same safe ceiling this note originally established for `.scene-world` as a whole.
- The project has no version control (`.git`) yet, which is a process risk, not a code risk — flagged in §27.

## 23. Confirmed requirements

- Static 3-file/9-asset Figma-derived structure, as inspected (§3–4).
- No build tooling, no framework, no JS currently present.
- Composition must be visually preserved; no redesign without explicit new requirements.
- Loading Screen, Global Movement, and Individual Interactivity are three independently stageable systems.
- Global movement = unified single-scene motion, explicitly not multi-depth parallax.
  > **SUPERSEDED (2026-08-18)** — replaced by per-layer depth-based parallax (sky/buildings/plaza each move at their own rate, buildings' depth derived mathematically from their frame position). Requested explicitly by the user in chat, overriding this bullet. See §10 and DEVELOPMENT_PLAN.md Stage 7 for the accepted replacement.
- Aircord.co.jp first-screen hero is the feel reference for global movement only.
- Buildings are the candidate interactive layer; exact interactive subset is undecided.
  > **SUPERSEDED (2026-08-18)** — resolved: all 7 buildings are interactive (hover confirmed and implemented, using real-silhouette hit-testing — see §12/§13).
- Global movement and local hover/click must be architecturally decoupled.
- Reduced-motion and mobile/touch must both be addressed, not skipped.
- Work proceeds one development stage at a time, with user review between stages.
- Loading Screen visual design, timing, readiness signal, and revisit behavior are all decided and implemented as a shared branded intro overlay (`intro.css`/`intro.js`) on both desktop and mobile. See §8 for the full resolved spec and DEVELOPMENT_PLAN.md Stage 4 for the shipped file list.
- Desktop fountain pool has a looping water video (`assets/вода.mp4`, its own already-lit CGI oval-pool render with jets/ripples) in place of the static painted water in `plaza.png`, masked to exactly the pool ring by the existing `assets/water-mask.svg` (unmodified — the same mask file created for, and left behind by, the earlier reverted CSS-shimmer fountain attempt in commit `bab66d7`/`c2adb62`). Decorative only, not a new interactive element — the GLONARI globe/pedestal and plaza click/hover behavior are unchanged, and plaza-interactivity (item 2 below) remains a separate, still-open question. Autoplay/muted/loop, paused under `prefers-reduced-motion` (per §21) rather than removed. Confirmed and implemented 2026-08-27 — see DEVELOPMENT_PLAN.md Stage 12 for the shipped implementation note.

## 24. Open questions

Requires your decision before the relevant stage can be implemented:
1. ~~Loading Screen: full visual design, progress strategy, minimum duration, exit transition, failure/error behavior, revisit behavior.~~
   > **SUPERSEDED (2026-08-25)** — resolved: see §8's full resolution and DEVELOPMENT_PLAN.md Stage 4's IMPLEMENTED note for what shipped.
2. Which specific buildings are interactive (subset of the 7), and whether the plaza is ever interactive.
   > **SUPERSEDED (2026-08-18)** — resolved: all 7 buildings. Plaza-interactivity remains open.
3. Exact hover visual spec (scale factor, glow color/intensity/blur, timing).
   > **SUPERSEDED (2026-08-18)** — glow color/intensity/blur and timing resolved and implemented (see §13). Scale factor remains open, deliberately deferred to a separate future step.
4. What clicking a building actually opens (panel vs modal vs something else) and what content it shows.
5. Exact mathematical spec for global movement (amplitude, easing, the "arc/curve" mechanism) — deferred by design to the Aircord analysis stage, but the final numbers are still your call.
6. Mobile/touch replacement behavior for cursor-driven movement and hover.
   > **SUPERSEDED (2026-08-21)** — resolved: no movement/hover equivalent on touch, ever. Static mobile background + tap-target hit zones per building, opening the same desktop modal (`#modal-overlay`/`#modal-card`) with that building's content. See §19 and DEVELOPMENT_PLAN.md Stage 14 for the full decision record; not yet implemented as of this date.
   > **SUPERSEDED (2026-08-21, later same day)** — implemented: see §19's IMPLEMENTED note and DEVELOPMENT_PLAN.md Stage 14 for what shipped and what verification (automated) still leaves open (real-device manual testing).
   > **SUPERSEDED (2026-08-24, later still)** — the tap-zone overlay this note describes was itself later replaced by a real swipe/tap carousel (§19's 2026-08-23 entries), and that carousel's swipe/tap did not actually work on a real touch device until now: an exact-zero tap-vs-drag check, a `touch-action` value that let the browser cancel non-horizontal swipes, and native image drag-and-drop breaking mouse-drag testing are all fixed, and tapping the active building now genuinely opens the modal instead of being a deliberate no-op. See §19's matching 2026-08-24 note and DEVELOPMENT_PLAN.md Stage 14 for full detail. Real-device manual testing remains the explicit outstanding item.
7. Whether image asset optimization (compression/format conversion) is in scope now or a later concern.
8. Whether to initialize git version control for this project going forward.
9. Whether the page ever scrolls, or is intended as a single fixed viewport-height "hero" experience only.

## 25. Technical assumptions

(Assumed temporarily for planning purposes only — not decisions, subject to your confirmation)
- Vanilla JavaScript (no framework, no bundler) will be used for interactivity, consistent with the current stack and the instruction not to introduce a framework "just because it's convenient." This should be explicitly confirmed with you before Stage 2.
- `requestAnimationFrame` (or CSS-only transitions where sufficient) will drive animation loops for performance.
- Movement and hover will be implemented via CSS `transform` (translate/scale) on dedicated wrapper elements to stay GPU-compositor-friendly, rather than animating `top/left/width/height`.
- The existing DOM structure can be extended (wrapper divs added around/within existing layers) rather than flattened or rebuilt.

## 26. Design assumptions

(Visual/behavioral assumptions, not confirmed)
- The "curved/arc-like" movement quality will likely require either a non-linear cursor-to-offset mapping or a combined translate+slight-rotate/skew treatment — the specific technique is not decided and depends on Stage 6's reference analysis.
- Loading Screen and main scene likely share the same visual "world" (colors/typography) even though its own design is undefined — this is a soft assumption only, not a spec.
  > **SUPERSEDED (2026-08-25)** — confirmed true, not just assumed: the implemented intro overlay reuses the scene's existing navy (`#0b1e3a`) and the same `--gold-bright` gold already used by the cursor/hover glow/section modal, rather than introducing a new palette. See §8.

## 27. Risks / unknowns

- **Asset weight:** 9 PNGs totaling ~18.5MB is heavy for a page that will also need a loading screen and smooth animation; this may force a performance/optimization stage (Stage 15) to matter more than usual.
  > **SUPERSEDED (2026-08-25)** — format/compression risk addressed: all scene PNGs (desktop + mobile) now have a WebP sibling served via `<picture>` with the PNG as fallback, ~87% smaller overall (~47.5MB → ~5.97MB), no visible quality loss. The "also needs a loading screen and smooth animation" half of this risk (Stage 4, still blocked; Stage 15's animation-loop profiling, still undone) is unaffected by this change. See DEVELOPMENT_PLAN.md Stage 15 and chat/prompt from 2026-08-25.
  > **SUPERSEDED (2026-08-25, later same day)** — "Stage 4, still blocked" above is now out of date: the loading screen (as a branded intro overlay) is decided and implemented, unblocked by this same asset-weight fix landing first. See §8 and DEVELOPMENT_PLAN.md Stage 4's IMPLEMENTED note.
- **Edge exposure during global movement:** because `.scene` is sized exactly to the composition with `overflow:hidden`, any naive translate of the whole scene will reveal empty background — needs a masking/oversizing architecture decision before Stage 7 can be implemented safely.
- **No version control:** without git, changes across stages can't be diffed/rolled back safely; recommend initializing git before Stage 2 (requires your confirmation — not done as part of this planning task).
- **`pointer-events:none` everywhere:** must be selectively reversed per interactive element without accidentally making decorative layers (sky/plaza) intercept clicks meant for buildings behind/around them, given the plaza sits visually on top of the buildings in DOM order.
- **Bespoke crop techniques** on `glonari-ancillary` and `plaza`: any future wrapper/transform architecture must not break these two custom offset/oversize crops, since they are not simple `object-fit:cover`.
- **Undefined interaction surface:** panels/modals, the interactive-building list, and the Loading Screen design are all currently undefined, which blocks detailed implementation planning for Stages 4, 9–11 until you provide those decisions.
  > **SUPERSEDED (2026-08-25)** — the Loading Screen portion of this risk is resolved: design decided and implemented (§8, DEVELOPMENT_PLAN.md Stage 4). Panels/modal content and the interactive-building subset (Stages 9–11) remain open, per §12/§15/§24.

## 28. Out-of-scope items for the current stage

- Any code implementation (explicitly excluded from this planning task).
- Any redesign of the existing Figma-derived composition.
- ~~Finalizing Loading Screen visual design (design not yet provided).~~
  > **SUPERSEDED (2026-08-25)** — no longer out of scope: finalized and implemented. See §8 and DEVELOPMENT_PLAN.md Stage 4.
- Finalizing which buildings are interactive, or panel/modal content.
- Image asset optimization/replacement.
  > **SUPERSEDED (2026-08-25)** — image optimization (format only, not replacement: originals kept as fallback) is now in scope and executed, see DEVELOPMENT_PLAN.md Stage 15 and chat/prompt from 2026-08-25.
- Introducing a JS framework or build tooling (pending confirmation this stays vanilla JS).
- Git initialization/version control setup (flagged as a recommendation only).
